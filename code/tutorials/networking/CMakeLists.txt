cmake_minimum_required(VERSION 2.6)

BFG_INIT_PACKAGE(TutorialNetworking)

FILE(GLOB SOURCES
	*.cpp
)

FILE(GLOB HEADERS
	*.h
)

INCLUDE_DIRECTORIES(${BoostLog_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${OGRE_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${OIS_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${MYGUI_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${BFG_ENGINE_INCLUDE_DIR})
INCLUDE_DIRECTORIES(..)	# For subdirectories
INCLUDE_DIRECTORIES(.)	# For subdirectories

ADD_EXECUTABLE(bfgTutorialNetworking ${SOURCES} ${HEADERS})

SET_TARGET_PROPERTIES(bfgTutorialNetworking PROPERTIES OUTPUT_NAME_DEBUG "bfgTutorialNetworking_d")

TARGET_LINK_LIBRARIES(bfgTutorialNetworking
	${Boost_LIBRARIES}
	${BoostLog_LIBRARIES}
	${OGRE_LIBRARIES}
	${MYGUI_LIBRARIES}
	${MYGUI_OGRE_LIBRARIES}
	bfgBase
	bfgController
	bfgCore
	bfgEventCore
	bfgModel
	bfgNetwork
	bfgPhysics
	bfgView
)

INSTALL(TARGETS bfgTutorialNetworking
	RUNTIME DESTINATION ${BFG_BIN_DIR}
	COMPONENT devel
)
